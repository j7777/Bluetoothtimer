<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ua.kh.ruschess.bluetoothtimer"
    android:installLocation="auto"
    android:versionCode="11"
    android:versionName="11.0">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BluetoothAdapterErrorActivity" />
        <activity android:name=".StepOneActivity" />
        <activity android:name=".StepTwoActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/action_settings" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".TimerActivityServer" />
        <activity android:name=".TimerActivityClient1" />
        <activity android:name=".TimerActivityClient2"></activity>
    </application>

</manifest>